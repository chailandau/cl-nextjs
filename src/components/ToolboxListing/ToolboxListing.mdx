import { ArgTypes, Canvas, Meta } from '@storybook/blocks';

import * as ToolboxStories from './ToolboxListing.stories';

<Meta of={ToolboxStories} />

# About Component

The `Toolbox` component features a section with a title and subheading. It contains a grid of tools consisting of icons and names.

<Canvas of={ToolboxStories.Default} />

<br />
<br />
<br />

### Props Table

<ArgTypes />
