import { ArgTypes, Canvas, Meta } from '@storybook/blocks';

import * as LinkStories from './Link.stories';

<Meta of={LinkStories} />

# Link Atom

The `Link` atom wraps the `next/link` component. There are lots of goodies built in, like prefetching. Read more about it [here](https://nextjs.org/docs/pages/api-reference/components/link).

<Canvas of={LinkStories.Default} />

<br />

## children

Text is supplied as a child to the `Link` atom.

## href

The link destination.

## className

Add an optional CSS class name.

## underline

Add a line under the link. On hover, line expands upwards to become a solid block. Defaults to `true`. If disabled, there will be no line under the link except on hover.

## icon

Add an optional arrow icon. Defaults to `true`.

<br />
<br />
<br />

## Props Table

<ArgTypes />
